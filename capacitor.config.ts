import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.restaurantos.app',
  appName: 'RestaurantOS AI',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
